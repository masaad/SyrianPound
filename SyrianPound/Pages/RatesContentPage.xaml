<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:il8n="clr-namespace:SyrianPound;assembly=SyrianPound"      
             xmlns:vm="clr-namespace:SyrianPound;assembly=SyrianPound"
             x:Class="SyrianPound.RatesContentPage"
             Title="{Binding TabName}"
             >
	<ContentPage.Content>
		<Grid>
			<Grid.Padding>
				<OnPlatform x:TypeArguments="Thickness"
    						iOS="5, 20, 0, 0" />
			</Grid.Padding>
			 <Grid.ColumnDefinitions>
			 	<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto"/> 
				<ColumnDefinition Width="Auto"/> 
			 </Grid.ColumnDefinitions>
			 <Grid.RowDefinitions>
		      <RowDefinition Height="Auto"  />
		      <RowDefinition Height="*" />
		      <RowDefinition Height="100" />
		    </Grid.RowDefinitions>	
		    <Label Grid.Row="0"
		           Grid.Column="0"
		           Grid.ColumnSpan="2"
		           Text="{Binding LastUpdate, StringFormat='Last Updated {0:MM/dd/yy H:mm:ss zzz}'}"/>				               	
		    		
			<Label Grid.Row="1" 
			       Grid.Column="0"
			       Text="{il8n:TranslateExtension LblCenteralBank}" />

		</Grid>
	</ContentPage.Content>
</ContentPage>
